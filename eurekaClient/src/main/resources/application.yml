server:
  port: 8010
#  当前服务注册在eureka中心的名字
spring:
  application:
    name: provider
#当前注册中心访问地址
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
#  是否将当前服务IP注册到中心
    prefer-ip-address: true